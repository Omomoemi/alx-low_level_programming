#include "main.h"
/**
 * _abs - computes absolute value of integer
 * @n: the int to be computed
 *
 * Return: the absolute value of int
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
