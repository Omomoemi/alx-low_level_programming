#include "main.h"
#include <stdio.h>

/**
 * main - Prints its name followed by a new line
 * @argc: The srgument that counts
 * @argv: The argument vector
 * Return: 0
 */
int main(int argc, char *argv[])
{       
        printf("%s\n", argv[argc *0]);
        
        return (0);
}
